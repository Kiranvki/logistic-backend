module.exports = Object.freeze({
    ctrl: require('../vehicle_master/vehicle_master.controller'), // exporting the controller 
    routes: require('../vehicle_master/vehicle_master.routes') // exporting the routes 
  });
  