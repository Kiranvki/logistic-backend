module.exports = Object.freeze({
    ctrl: require('./vehicle_model_master.controller'), // exporting the controller 
    routes: require('./vehicle_model_master.router') // exporting the routes 
  });
  