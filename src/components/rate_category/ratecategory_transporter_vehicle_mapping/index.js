module.exports = Object.freeze({
    ctrl: require('./ratecategory_transporter_mapping.controller'),// exporting the controller 
    routes:require('./ratecategory_transporter_mapping.routes') // exporting the routes 
    })