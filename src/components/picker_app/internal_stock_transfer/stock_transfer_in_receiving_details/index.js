module.exports = Object.freeze({
  ctrl: require('./stock_transfer_in_receiving_details.controller'), // exporting the controller 
  routes: require('./stock_transfer_in_receiving_details.router') // exporting the routes 
});
