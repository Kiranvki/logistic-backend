module.exports = Object.freeze({
  ctrl: require('./purchase_order_recieving_details.controller'), // exporting the controller 
  routes: require('./purchase_order_recieving_details.router') // exporting the routes 
});
