module.exports = Object.freeze({
  ctrl: require('./app_picker_user_attendance.controller'), // exporting the controller 
  routes: require('./app_picker_user_attendance.router') // exporting the routes 
});
