module.exports = Object.freeze({
  ctrl: require('./invoice_pickerboysalesorder_mapping.controller'), // exporting the controller 
  routes: require('./invoice_pickerboysalesorder_mapping.router') // exporting the routes 
});
