module.exports = Object.freeze({
  ctrl: require('./pickerboy_salesorder_mapping.controller'), // exporting the controller 
  routes: require('./pickerboy_salesorder_mapping.router') // exporting the routes 
});
