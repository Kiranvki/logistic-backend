module.exports = Object.freeze({
    ctrl: require('./app_delivery_user_attendance.controller'), // exporting the controller 
    routes: require('./app_delivery_user_attendance.routes') // exporting the routes 
  });
  