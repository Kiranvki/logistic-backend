module.exports = Object.freeze({
    ctrl: require('./transporter_master_mapping.controller'), // exporting the controller 
    routes: require('./transporter_master_mapping.routes') // exporting the routes 
  });
  