module.exports = Object.freeze({
    ctrl: require('./transporter_vehicle_mapping.controller'), // exporting the controller 
    routes: require('./transporter_vehicle_mapping.routes') // exporting the routes 
  });
  