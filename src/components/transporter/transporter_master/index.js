module.exports = Object.freeze({
    ctrl: require('./transporter_master.controller'), // exporting the controller 
    routes: require('./transporter_master.router') // exporting the routes 
  });
  