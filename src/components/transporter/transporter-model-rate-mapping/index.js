module.exports = Object.freeze({
    ctrl: require('./transporter-model-rate-mapping.controller'), // exporting the controller 
    routes: require('./transporter-model-rate-mapping.routes') // exporting the routes 
  });
  