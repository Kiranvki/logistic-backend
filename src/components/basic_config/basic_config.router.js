// user controller 
const ctrl = require('./basic_config.controller');
// custom joi validation
const {

} = require('./basic_config.validators');

// exporting the user routes 
function userRoutes() {
  //open, closed
  return (open) => {
    // closed

  };
}

module.exports = userRoutes();
