module.exports = Object.freeze({
    ctrl: require('./checked_in_vehicles.controller.js'), // exporting the controller 
    routes: require('./checked_in_vehicles.router.js') // exporting the routes 
  });