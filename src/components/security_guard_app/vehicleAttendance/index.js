module.exports = Object.freeze({
    ctrl: require('./vehicleAttendance.controller.js'), // exporting the controller 
    routes: require('./vehicleAttendance.router.js') // exporting the routes 
  });