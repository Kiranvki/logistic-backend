module.exports = Object.freeze({
  ctrl: require('./app_security_guard_attendance.controller'), // exporting the controller 
  routes: require('./app_security_guard_attendance.router') // exporting the routes 
});
