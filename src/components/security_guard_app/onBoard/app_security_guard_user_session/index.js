module.exports = Object.freeze({
  ctrl: require('./app_security_guard_user_session.controller'), // exporting the controller 
  routes: require('./app_security_guard_user_session.router') // exporting the routes 
});
