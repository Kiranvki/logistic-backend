module.exports = Object.freeze({
  ctrl: require('./app_picker_user_session.controller'), // exporting the controller 
  routes: require('./app_picker_user_session.router') // exporting the routes 
});
